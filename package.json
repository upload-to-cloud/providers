{
  "name": "package-based",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many --t build",
    "build:affected": "npx nx affected -t build"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "16.1.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
